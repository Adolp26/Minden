name: Deploy to AWS

on:
  push:
    branches:
      - main  # O deploy será feito quando um push for feito na branch 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Compile TypeScript
        run: npx tsc

      - name: Create .env file (using secrets)
        run: |
          echo "HOST=localhost" >> .env
          echo "USER=${{ secrets.DB_USER }}" >> .env
          echo "PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
          echo "PORT=5432" >> .env
        env:
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

      - name: SSH into AWS EC2 instance and deploy
        uses: appleboy/ssh-action@v0.1.0
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ${{ secrets.AWS_USER }}
          key: ${{ secrets.AWS_SSH_KEY }}
          script: |
                echo "Stopping the current application..."
                cd /home/ubuntu/Minden/api-minden  # Caminho correto para o seu app
                pm2 stop app || true  # Para a aplicação, caso esteja rodando
                echo "Pulling the latest changes from Git..."
                git pull origin main
                echo "Installing dependencies..."
                npm install
                echo "Compiling TypeScript..."
                npx tsc
                echo "Starting the application..."
                npm start &
